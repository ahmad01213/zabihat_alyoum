import 'package:flutter/material.dart';

class MazadScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
        child: Container(
      color: Colors.lightGreen,
    ));
  }
}
